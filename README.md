shows the dialog when an form detect shows the fill button to fill the data

inject into the layout.tsx file or page.tsx file in root or parent level
<><FormAutoFill useRandomData={true} />{children}</>
